extends layouts/main
include mixins/itemBlock

block content
    .home-items
        each item in items 
            +itemBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                itemFile:item.fileUrl
            })